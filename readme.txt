клонирование проекта в папку nameProject:
git cloned https://... nameProject

Добавить файлы в коммит:
git add .

Записать коммит:
git commit -m "Комментарий коммита"

Отправить коммит:
git push

принять изменения:
git pull

авторство коммитов:
git config --global user.email "ваш емайл"
git config --global user.name "ваше имя"
